<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_centerHorizontal="true"
    android:layout_centerVertical="true"
    android:layout_margin="2dp"
    android:minHeight="@dimen/list_film_height"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/image_film"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:id="@+id/text_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@id/image_film"
        android:layout_weight="3"
        android:orientation="vertical"
        android:paddingLeft="8dp">

        <TextView
            android:id="@+id/tv_judul"
            style="@style/TextViewItems.Judul"
            tools:text="Judul Film" />

        <TextView
            android:id="@+id/tv_deskripsi"
            style="@style/TextViewItems.Deskripsi"
            tools:text="Deskripsi" />

        <TextView
            android:id="@+id/tv_jadwal"
            style="@style/TextViewItems.Jadwal"
            tools:text="Jadwal Film" />
    </LinearLayout>
</RelativeLayout>